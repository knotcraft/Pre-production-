{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth.uid != null",
        ".write": "auth.uid != null"
      }
    },
    "invitations": {
      ".read": "auth.uid != null",
      ".write": "auth.uid != null"
    }
  }
}
